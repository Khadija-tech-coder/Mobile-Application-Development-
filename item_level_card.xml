<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="5dp">

    <LinearLayout
        android:id="@+id/card_level_root"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="@drawable/bg_aurora_level_normal"
        android:elevation="3dp"
        android:paddingBottom="8dp">

        <!-- Thumbnail area -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="90dp">

            <ImageView
                android:id="@+id/iv_level_thumb"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerCrop" />

            <!-- Lock dim overlay -->
            <View
                android:id="@+id/lock_overlay"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="#AA000000"
                android:visibility="gone" />

            <!-- Lock icon (centred) -->
            <TextView
                android:id="@+id/tv_lock_icon"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:text="🔒"
                android:textSize="28sp"
                android:visibility="gone" />

            <!-- Badge: HARD / ✓ (top-end corner) -->
            <TextView
                android:id="@+id/tv_level_badge"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="top|end"
                android:layout_margin="4dp"
                android:background="@drawable/bg_badge"
                android:padding="4dp"
                android:textColor="@android:color/white"
                android:textSize="10sp"
                android:visibility="gone" />

        </FrameLayout>

        <!-- Level number -->
        <TextView
            android:id="@+id/tv_level_number"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:paddingTop="6dp"
            android:textColor="@color/white"
            android:textSize="15sp"
            android:textStyle="bold" />

        <!-- Best moves chip (only visible when completed) -->
        <TextView
            android:id="@+id/tv_best_moves_card"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="2dp"
            android:paddingStart="6dp"
            android:paddingEnd="6dp"
            android:paddingTop="1dp"
            android:paddingBottom="1dp"
            android:background="#3345A29E"
            android:textColor="#45A29E"
            android:textSize="10sp"
            android:visibility="gone" />

    </LinearLayout>
</FrameLayout>
